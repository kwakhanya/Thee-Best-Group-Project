export const WINDOW_WIDTH = window.innerWidth || document.getElement.clientWidht || document.body.clientWidth
export const WINDOW_HEIGHT = window.innerHeight || document.getElement.clientHeight || document.body.clientHeight
export const WORLD_SIZE = {width: 1600, height: 1200}
export const ASSETS_URL = '../assets'